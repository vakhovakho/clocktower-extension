// chrome.runtime.onMessage.addListener(
//     function(request, sender, sendResponse) {
//         if (request.gameEnded) {
//             console.log('localStorage change detected', request.key, request.newValue);
//             // Handle the change
//         }
//     }
// );
